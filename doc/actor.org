* actor
:PROPERTIES:
:ID:       91c7ba77-bb5c-482a-93d2-3e1466a01e22
:END:

A program providing tools to manipulate the files in the project.

** actor#,install-node-binaries
:PROPERTIES:
:ID:       220179a9-aa3e-490d-ab6e-3ef02d759bee
:END:

Premises
- The [[ref:6612d34b-e5e7-4d42-b1bf-d73bb1ed9906][environment]] is started.
- actor#,install-node-binaries

Conclusion
- npm dependencies are installed.

** actor#,image-init-build
:PROPERTIES:
:ID:       ead19c86-d75c-45d6-8aa7-00cea2f049b0
:END:

Premises
- The [[ref:6612d34b-e5e7-4d42-b1bf-d73bb1ed9906][environment]] is started.
- actor#,image-init-build

Conclusion
- An initial VM image has been built configured to run on the local machine and ready to
  be deployed to.

** actor#,vm-init-start
*** Private
**** VM spec
:PROPERTIES:
:ID:       9952e0a0-6fe0-40cb-97fd-1d56074ab2a5
:END:

*Guest Virtual Machine Specifications*

- Architecture :: x86_64 virtual machine
- Chipset & Acceleration :: Modern Q35 PCIe-based machine model with full KVM hardware virtualization acceleration (near-native performance)
- CPU Model :: Exact host CPU feature passthrough (complete CPUID match including AVX-512 and all host extensions; zero emulation overhead for supported instructions)
- Memory Allocation :: Exactly 8 GiB of guest physical RAM
- CPU Topology :: Exactly 4 virtual CPUs visible inside the guest (configured as 1 socket with 1 thread per core)
- Storage Backend :: QCOW2 disk image; all guest writes are collected into a temporary overlay that is automatically discarded on VM exit (original image remains completely unchanged)
- Block Device :: Paravirtualized virtio-blk-pci device connected to the QCOW2 image (high-throughput, low-overhead I/O)
- Networking Backend :: User-mode (SLIRP/NAT) networking with three explicit TCP port forwards from host to guest:
  - SSH: host port → guest port 22
  - HTTP: host port → guest port 80
  - HTTPS: host port → guest port 443
- Network Device :: Paravirtualized virtio-net-pci interface with checksum offload and multi-queue support
- Control Interface :: QEMU Machine Protocol (QMP) monitor exposed on a Unix domain socket; external tools can connect and issue JSON commands (query-status, system_powerdown, etc.)

*Host Process & Management Specifications*

- Execution Context :: Runs as a transient systemd user unit (named after the supplied unit variable)
- Environment Inheritance :: Full user environment, cgroups, and resource limits
- Lifecycle Behavior :: Automatic cleanup on user logout unless remain-after-exit is explicitly requested

*** Public
:PROPERTIES:
:ID:       518d54c7-3288-4b71-a951-e612dfab5cb2
:END:

Premises
- The current shell runs OUTSIDE of the container
- [[ref:ead19c86-d75c-45d6-8aa7-00cea2f049b0][actor#,image-init-build]] has been run.
- actor#,vm-init-start

Conclusion
- A VM is running locally as if it was a VPS ready to be deployed to in production.
- An ssh connection to the VM is opened

** actor#,vm-deploy
:PROPERTIES:
:ID:       62d66eb4-c006-4f3b-86a0-1675191325cb
:END:

Premises
- [[ref:518d54c7-3288-4b71-a951-e612dfab5cb2][actor#,vm-init-start]] has been run.
- The [[ref:6612d34b-e5e7-4d42-b1bf-d73bb1ed9906][environment]] is started.
- os-name :≡ "init"
- actor#,vm-deploy os-name

Conclusion
- The OS associated to the OS specification named os-name has been deployed to the
  VM.

** actor#,ca-build
:PROPERTIES:
:ID:       bdfdc0d0-6b2a-45a5-b4ca-17b224087d68
:END:

Premises
- actor#,ca-build

Conclusion
- A local CA key and self-signed CA certificate exist under =etc/certs/=.
- A leaf certificate for =localhost= signed by the local CA exists under =etc/certs/=.
- The local CA has been registered in the NSS database so that Chrome accepts HTTPS
  connections to =localhost=.

** actor#,synchronize-with-upstream
:PROPERTIES:
:ID:       c5d59ff9-cc78-4f98-a7f6-22d19c39caff
:END:

Premises
- remote upstream must exist

Conclusion
- The local master branch points to the same commit as the remote
  - upstream/master branch,
  - origin/master branch.

** actor#,todos
:PROPERTIES:
:ID:       a3f2c1d8-7e4b-4f9a-b6c5-2d1e8f3a5b9c
:END:

Premises
- The [[ref:6612d34b-e5e7-4d42-b1bf-d73bb1ed9906][environment]] is started.
- actor#,todos

Conclusion
- All TODO comments in the project are listed.
